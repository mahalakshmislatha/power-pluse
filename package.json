{
  "name": "power-pulse",
  "version": "1.0.0",
  "description": "Smart Power Outage Tracker for Tamil Nadu",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
